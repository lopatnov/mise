# Production secrets — edit before deploying!
# Generate JWT_SECRET: openssl rand -hex 32
# APP_URL is used in password-reset email links.

APP_URL=http://YOUR_SERVER_IP
JWT_SECRET=replace_with_a_random_secret
JWT_EXPIRES_IN=7d
