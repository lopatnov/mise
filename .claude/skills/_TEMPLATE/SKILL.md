> Это канонический шаблон скилла — не настоящий вызываемый скилл (frontmatter ниже намеренно в
> `yaml`-блоке, не в реальном виде, чтобы эта директория не регистрировалась как выбираемый скилл
> `_TEMPLATE`). Копируй директорию при создании нового скилла, перенеси YAML-блок в настоящий
> frontmatter (`---` первой строкой файла). Тело шаблона ниже — чек-лист/плейбук, который дирижёр
> или агент применяет пошагово, не системный промпт роли (для ролей — `agents/_TEMPLATE.md`).

# Skill Name — краткая суть (шаблон, не реальный скилл)

```yaml
---
name: skill-name
description: Когда применять этот скилл — конкретный триггер (запрос пользователя, сигнал от repo-scout, регулярный цикл). Это поле дирижёр видит при выборе скилла.
---
```

## Когда применять

Конкретные триггеры.

## Когда НЕ применять

Явные границы — что выглядит похоже, но не этот случай.

## Чек-лист / шаги

Пошаговый план применения.

## Связанные роли и правила

Кто выполняет шаги, на какие `rules/*`/`agents/*` опирается.

## Definition of Done

Критерий завершённости применения скилла.
